# Cloud Dashboard - Kubernetes Ingress
# Uncomment and configure domain after deploying
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: cloud-dashboard
#   namespace: cloud-dashboard
# spec:
#   rules:
#     - host: dashboard.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: frontend
#                 port:
#                   number: 80
